---
import type { GetStaticPaths } from "astro";
import { siteList } from "../../feed/list";

export const getStaticPaths = (() =>
  siteList.map((site) => ({ params: { site: site.param } }))) satisfies GetStaticPaths;

return Astro.redirect(`/feed/${Astro.params.site}/1`, 301);
---
